<%= form_for(@project) do |f| %>
	<div class="field">
		<%= f.text_area :title %><br>
		<%= f.text_area :description %><br>
		<%= f.file_field :image %><br>
	</div>
	<div class="action">
		<%= f.submit "Create" %>
	</div>
<% end %>